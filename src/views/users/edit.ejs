<!DOCTYPE html>
<html lang="en">
<!-- HEAD -->
<%- include('../partials/head') %>

    <body class="body-form">
        <main class="main-form">
            <section class="section-form" style="padding-bottom: 5%;">
                <h4>EDITAR CUENTA</h4>
                <form action="/users/update/<%= user.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="form-reg">
                        <label for="name">NOMBRE</label>
                        <input placeholder="Daniel" type="text" name="name" id="name"
                            value="<%= locals.old && old.name ? old.name : null %>"
                            class="form-control <%= locals.errors && errors.name ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.name ? errors.name.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="surname">APELLIDO</label>
                        <input placeholder="Martinez" type="text" name="surname" id="surname"
                            value="<%= locals.old && old.surname ? old.surname : null %>"
                            class="form-control <%= locals.errors && errors.surname ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.surname ? errors.surname.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="email">CORREO ELECTRONICO</label>
                        <input placeholder="mi@gmail.com" type="text" name="email" id="email"
                            value="<%= locals.old && old.email ? old.email : null %>"
                            class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.email ? errors.email.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="password">CONTRASEÑA</label>
                        <input placeholder="********" type="password" name="password" id="password"
                            value="<%= locals.old && old.password ? old.password : null %>"
                            class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.password ? errors.password.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="country">PAÍS</label>
                        <input placeholder="Argentina" type="text" name="country" id="country"
                            value="<%= locals.old && old.country ? old.country : null %>"
                            class="form-control <%= locals.errors && errors.country ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.country ? errors.country.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="province">PROVINCIA</label>
                        <input placeholder="Buenos Aires" type="text" name="province" id="province"
                            value="<%= locals.old && old.province ? old.province : null %>"
                            class="form-control <%= locals.errors && errors.province ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.province ? errors.province.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <label for="city">CIUDAD</label>
                        <input placeholder="Escobar" type="text" name="city" id="city"
                            value="<%= locals.old && old.city ? old.city : null %>"
                            class="form-control <%= locals.errors && errors.city ? 'is-invalid' : null %>">
                        <p class="text-danger">
                            <%= locals.errors && errors.city ? errors.city.msg : null %>
                        </p>
                    </div>
                    <div class="form-reg">
                        <button class="" type="submit">EDITAR</button>
                    </div>
                </form>
            </section>
        </main>

    </body>

</html>